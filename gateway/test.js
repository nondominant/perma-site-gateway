create auction
search and view auctions
make purchase


problem: 
store sites as text in single files and serve through root node replacement
-multiple " ' ` problem, `'" could be solved by uploading files in base64 directly, then you have the problem of <script> tags not executing when the root node is replaced.


toolchain that compiles a modern web project into single site with DNS-less navigation

app can essentially be a gateway with hard coded search query

